<li>
  <%= link_to sub_category.name, filter_posts_path(sub_category.slug) %>
  <ul>
    <%- sub_category.children.ordered.each do |sub_category| %>
      <%= render :partial => 'sub_category', :locals => {:sub_category => sub_category} %>
    <% end %>
  </ul>
</li>